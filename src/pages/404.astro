---
import SitePage from '../components/SitePage.astro';
import { readRoutes } from '../governance/routes.mjs';
const route = readRoutes().find(r => r.path === '/404.html')!;
---
<SitePage route={route} review={false} />
