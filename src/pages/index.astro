---
import Page from '../components/WP4Review.astro';
import { readRoutes } from '../governance/routes.mjs';
const route = readRoutes().find(r => r.path === '/')!;
---
<Page route={route} review={false} />
