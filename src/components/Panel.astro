---
interface Props { title: string; id: string }
const { title, id } = Astro.props;
---
<section class="panel" aria-labelledby={id}>
  <h2 class="panel__title" id={id}>{title}</h2>
  <div class="panel__body"><slot /></div>
</section>
