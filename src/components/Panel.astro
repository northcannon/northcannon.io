---
// Thin alias over Module so existing pages pick up the module look.
import Module from './Module.astro';
interface Props { title: string; id: string }
const { title, id } = Astro.props;
---
<Module id={id} title={title} class="panel"><slot /></Module>
